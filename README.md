 🩺 System Health Check Script (Cross-Platform)

This project is a **Bash-based system monitoring tool** that works on **Linux**, **macOS**, and **Windows (Git Bash / WSL)**.  
It checks essential system metrics like uptime, CPU load, memory, disk usage, and running services, displaying them in the terminal and logging to a file for later review.  

---



---

## 🚀 Features

✅ Shows **system date & time**  
✅ Displays **uptime** (Linux/Windows compatible)  
✅ Checks **CPU load percentage**  
✅ Displays **memory and disk usage**  
✅ Lists **top 5 memory-consuming processes**  
✅ Verifies if services (`nginx`, `sshd`, `docker`) are running  
✅ Saves results to `healthlog.txt` with a timestamp  
✅ Works on both **Linux** and **Windows (Git Bash)**  

---

## ⚙️ Setup and Usage

### 1️⃣ Clone the Repository
```bash
git clone https://github.com/YOUR-USERNAME/healthcheck.git
cd healthcheck
